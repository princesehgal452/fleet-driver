import React from 'react';
import { mount } from 'enzyme';
import FinalPage from '../FinalPage';


describe('OnboardNewDriverModal', () => {
  // let wrapper: ShallowWrapper;
  test('Should contain user\'s name', () => {
    // const wrapper = mount(
    //   <FinalPage
    //     onClose={() => null}
    //   />);
    // const inst = wrapper.find(FinalPage);
    // expect(inst.text()).toContain('Test User');
    // expect(inst.text()).not.toContain('Okay');
  });
  test('Should show close button if onClose function provided', () => {
    // const wrapper = mount(
    //   <FinalPage
    //     onClose={() => null}
    //   />);
    // const inst = wrapper.find(FinalPage);
    // expect(inst.text()).toContain('Okay');
  });
});
