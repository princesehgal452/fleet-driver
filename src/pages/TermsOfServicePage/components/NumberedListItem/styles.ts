import { Theme } from '@material-ui/core/styles';

const styles = (theme: Theme) => ({
  numberListItem: {
    display: 'list-item',
    padding: '0 0 8px 4px',
  },
});

export default styles;
