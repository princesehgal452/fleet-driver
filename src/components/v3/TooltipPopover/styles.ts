const styles = (theme) => ({
  contentBox: {
    verticalAlign: 'middle'
  },
  typography: {
    padding: theme.spacing(2),
    backgroundColor: theme.palette.confirmation,
    color: theme.palette.common.white,
  },
});

export default styles;
