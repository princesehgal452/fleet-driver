const styles = (theme) => ({
  numberListItemText: {
    color: theme.custom.colors.nightLight,
    fontSize: 12,
  },
  numberListItemTextWrapper: {
    margin: 0,
  },
  numberListItemHeaderText: {
    color: theme.custom.colors.nightLight,
    fontSize: 12,
    fontWeight: 500,
    paddingBottom: theme.spacing(1),
  },
});

export default styles;
