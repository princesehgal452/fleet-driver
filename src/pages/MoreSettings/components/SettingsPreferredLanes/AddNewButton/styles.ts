import { makeStyles } from '@material-ui/core';

const styles = () => ({
  icon: {
    width: 28,
    height: 28,
  },
  wrapper: {
    paddingTop: 16,
    paddingRight: 8,
    paddingBottom: 16,
    paddingLeft: 8,
  },
  label: {
    fontSize: 14,
    fontWeight: 500,
  },
});

export default makeStyles(styles);
