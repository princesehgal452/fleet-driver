const styles = (theme) => ({
  numberList: {
    fontSize: 12,
    listStyleType: 'decimal',
    marginLeft: 14,
  },
  headerList: {
    color: theme.custom.colors.nightDark,
    fontSize: 14,
    fontWeight: 500,
  },
});

export default styles;
